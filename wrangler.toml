name = "zanflow"
main = "src/index.js"
compatibility_date = "2024-09-23"

# Secrets — set these via CLI, never commit real values:
#   npx wrangler secret put DATABASE_URL
#
# For local dev, create a .dev.vars file (git-ignored) with:
#   DATABASE_URL=postgres://...
